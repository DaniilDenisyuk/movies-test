//readRecords("file.txt", ['Title', 'Release Date',...], '/n/n', ':')
import fs from "fs";

export const readRecords = (
  file,
  keys,
  recordsSeparator = "/n/n",
  keyValueSeparator = ":"
) => {};
